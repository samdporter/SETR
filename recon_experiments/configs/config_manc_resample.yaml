# Resampling Configuration for MANC Phantom Data
# Resample SPECT HKEM reconstruction to PET space for use as emission guidance

# Data directories
data_dir: "/home/storage/prepared_data/phantom_data/manc_nema_phantom_data"
results_dir: "/home/sam/working/synergistic_recon/results/manc_bootstraps/hkem"

# Resampling paths (will be updated by pipeline script)
pet_dir: "${data_dir}/PET/bootstraps/nonparametric/prompts_nonparam_s73_sf0.05_000"
spect_dir: "${data_dir}/SPECT"
spect_reconstruction: "${results_dir}/spect_hkem/reconstruction_x.hv"
transform_file: "${data_dir}/SPECT/spect2pet.nii"
output_file: "${data_dir}/PET/bootstraps/nonparametric/prompts_nonparam_s73_sf0.05_000/spect.hv"

use_2bpos: false  # Single bed position for bootstrap PET data
use_tof: false
flip: true  # IMPORTANT: Flip SPECT for MANC data

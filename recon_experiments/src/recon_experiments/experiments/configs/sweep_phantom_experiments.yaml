# Sweep configuration for phantom experiments
# Runs all 20 combinations: 4 phantoms × 5 algorithms

sweep_name: "phantom_experiments"
experiment_type: "phantom_algorithm"  # Special type for phantom experiments

# SGE resource requirements
sge:
  runtime: "48:00:00"  # 48 hours per experiment
  memory: "60G"        # 60GB memory per job
  cores: 1             # 1 core per job
  queue: null          # Use default queue
  gpu: true            # Enable GPU

# Phantom and algorithm combinations
# This will create a 4x5 grid = 20 experiments
phantoms:
  - manc
  - manc_short
  - anthro
  - nema

algorithms:
  - hkem
  - dtnv
  - tnv
  - log_dtnv
  - log_tnv

# Optional overrides for all experiments
# Uncomment and modify as needed
config_overrides: {}
  # num_epochs: 100
  # alpha: 0.02
  # beta: 0.02

# Resampling Configuration - 1 Bed Position
# Configuration for resampling SPECT reconstruction to PET space

# Data paths (these can be overridden via --override)
data_dir: "/home/storage/prepared_data/phantom_data/anthropomorphic_phantom_data"
results_dir: "/home/sam/working/synergistic_recon/results"

# Resampling paths (modify these for your specific dataset)
pet_template: "/home/storage/prepared_data/phantom_data/anthropomorphic_phantom_data/PET/phantom/template_image.hv"
spect_reconstruction: "/home/sam/working/synergistic_recon/results/hkem_spect_1bpos/reconstruction_x.hv"
transform_file: "/home/storage/prepared_data/phantom_data/anthropomorphic_phantom_data/SPECT/phantom_140/spect2pet.nii"
output_file: "/home/storage/prepared_data/phantom_data/anthropomorphic_phantom_data/PET/phantom/spect.hv"

# Alternative output for different datasets
# Uncomment and modify as needed:
# pet_template: "${data_dir}/PET/phantom/template_image.hv"
# spect_reconstruction: "${results_dir}/hkem_spect_1bpos/reconstruction_x.hv"
# transform_file: "${data_dir}/SPECT/phantom_140/spect2pet.nii"
# output_file: "${data_dir}/PET/phantom/spect.hv"